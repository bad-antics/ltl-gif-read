// examples/demo.ltl — runnable demo for ltl_gif_read

import std::io
use ltl_gif_read as lib

fn main() {
    io::println("ltl_gif_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}