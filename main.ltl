// ltl-gif-read/main.ltl
//
// GIF87a/89a LZW decoder with animation frame walker.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-gif-read v0.1.0")
    io::println("GIF87a/89a LZW decoder with animation frame walker.")
    0
}
